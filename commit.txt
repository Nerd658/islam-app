[2026-08-02] — Feature: Upgrade Arabic Audio Engine to Howler.js HTML5 Streaming

What changed:
- Installed `howler` package in `FRONTEND/islam app`.
- Rewrote `src/utils/audio.js` using `Howl` with `html5: true` streaming and automatic `onplayerror` / `onloaderror` fallback to `SpeechSynthesis` and phonetic reader.

Why:
- Delivers a rock-solid, production-grade audio engine that automatically unlocks audio contexts on iOS/Android, manages memory lifecycle, and guarantees audible sound across all browsers.

Files affected:
- FRONTEND/islam app/package.json
- FRONTEND/islam app/package-lock.json
- FRONTEND/islam app/src/utils/audio.js

Breaking changes:
- None.
