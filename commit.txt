[2026-08-02] — UI/UX: Preserve Tajweed letter colors on active word highlight & remove icon

What changed:
- Removed the Sparkles icon next to "Règles Tajweed :".
- Refactored active word highlight in `QuranReader.jsx` to render individual Tajweed HTML tokens (`dangerouslySetInnerHTML={{ __html: wHtml }}`).
- The active word now uses a subtle glowing green underline (`border-b-2 border-emerald-400`) and dark emerald backdrop (`bg-emerald-950/70`), preserving all letter-level Tajweed colors (Madd, Ghunna, Qalqala, Ikhfa, etc.) completely visible during recitation.

Why:
- Fixes Tajweed color occlusion so users can observe Tajweed pronunciation rules while tracking word-by-word recitation.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
