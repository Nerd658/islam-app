[2026-08-02] — UI/UX: Optimize full-height layout on Quran page to eliminate bottom whitespace

What changed:
- Updated outer wrapper and card container in `QuranReader.jsx` to use flex-1 vertical layout (`min-h-[calc(100vh-2rem)] md:min-h-screen` and `flex-1 overflow-y-auto`).
- The verses list now dynamically expands to fill 100% of available screen height down to the bottom without leaving blank empty space below the card.

Why:
- Fixes layout gap on `/quran` route so the reader card seamlessly fits full viewport height across mobile and desktop.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
