[2026-08-01] — UI: Add 99 Names to navigation menu

What changed:
- Added the `/names` route to the `navItems` array in `Navigation.jsx`.
- Associated the route with the `Star` icon from `lucide-react`.

Why:
- The 99 Names feature was accessible via routes but missing from the main sidebar/bottom nav, making it hard to discover.

Files affected:
- FRONTEND/islam app/src/components/Navigation.jsx

Breaking changes:
- None.
