[2026-08-02] — Feature: Persist Tasbih sessions across browser reloads

What changed:
- Configured `localStorage` persistence in `Tasbih.jsx` for current count, total session count, selected phrase, and audio sound toggle.
- Added session reset button to reset total counter when desired.

Why:
- Prevents accidental loss of Tasbih count when refreshing the browser page or navigating away.

Files affected:
- FRONTEND/islam app/src/pages/Tasbih.jsx

Breaking changes:
- None.
