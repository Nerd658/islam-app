[2026-08-02] — Feature: Add /api/tts Proxy Endpoint for Crystal Clear Arabic Letter Audio Streaming

What changed:
- **BACKEND/app.js**: Added `/api/tts` proxy route that streams Google TTS audio with server-side User-Agent headers, completely bypassing browser CORS restrictions.
- **src/utils/audio.js**: Connected `playArabicAudio` to the backend `/api/tts` proxy for clear, natural MP3 streaming for all letters and vowels.

Why:
- Eliminates robotic browser Web Speech voices and resolves CORS blocking by serving clean audio MP3 streams directly from the backend.

Files affected:
- BACKEND/app.js
- FRONTEND/islam app/src/utils/audio.js

Breaking changes:
- None.
