[2026-08-02] — UI/UX: Re-architect Quran Reader into two physically separate DOM containers

What changed:
- Separated `QuranReader.jsx` into 2 distinct physical DOM containers:
  1. Container 1 (Header Box): `flex-shrink-0` containing "Retour aux Sourates", "FR", "Tajweed", "Télécharger", "Règles Tajweed", and "Surah Title". This box has no scrollbar and CANNOT move.
  2. Container 2 (Verses Box): `flex-1 overflow-y-auto min-h-0`. This box is the ONLY scrolling container for verses.
- Restricted outer parent wrapper to `h-[calc(100vh-4rem)] overflow-hidden` to prevent page-level scrolling.

Why:
- Guarantees 100% physical separation so the header box never scrolls or disappears, while verses scroll independently in their dedicated box.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
