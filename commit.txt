[2026-08-07] — Enhancement: Rich UI for Knowledge Tutorials

What changed:
- Completely rewrote `knowledge.json` to structure tutorial steps as objects rather than plain strings.
- Added properties to each step: `title`, `description`, `isFard` (boolean), and `repetitions` (number).
- Updated `Knowledge.jsx` to render a rich, modern timeline UI for the tutorials.
- Added visual badges for 'Obligatoire (Fard)' (styled in red) and 'Recommandé (Sunnah)' (styled in blue).
- Added repetition counters (e.g., '3x') for steps like washing hands or face.

Why:
- The initial implementation was too basic (plain text list). The user specifically requested a true "step-by-step" tutorial experience with Fard/Sunnah distinctions and repetition counters, as outlined in the earlier research phase. This brings the UI up to the expected standard.

Files affected:
- src/data/knowledge.json — Restructured data schema.
- src/pages/Knowledge.jsx — Refactored `TutorialCard` component for rich UI rendering.

Breaking changes:
- None.

Notes for review:
- Check the Ablutions (Wudu) and Salat tabs on `/knowledge` to see the new badges and step formatting.
