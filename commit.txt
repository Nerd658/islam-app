[2026-08-01] — Bugfix: Fix Tasbih page crash and remove emoji

What changed:
- Fixed an `Uncaught ReferenceError` on the `/tasbih` page where buttons were bound to undefined `increment` and `reset` functions instead of `handleTap` and `handleReset`.
- Added the missing `RotateCcw` import from `lucide-react`.
- Replaced the sticker emoji (📿) with an `Activity` SVG icon from `lucide-react`.

Why:
- The Tasbih page was crashing entirely (white screen of death) due to these JavaScript reference errors. 

Files affected:
- FRONTEND/islam app/src/pages/Tasbih.jsx

Breaking changes:
- None.
