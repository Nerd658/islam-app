[2026-08-01] — Config: Launch Backend server on port 4000 and update Frontend .env

What changed:
- Configured backend default port to 4000 in `BACKEND/config/env.js` (port 3001 had conflicts).
- Updated `VITE_BACKEND_URL` in `FRONTEND/islam app/.env` to `http://localhost:4000`.
- Started Express backend server process in background.

Why:
- Allows the virtual Imam chatbot and other API features to communicate properly between frontend and backend.

Files affected:
- BACKEND/config/env.js
- FRONTEND/islam app/.env

Breaking changes:
- None.
