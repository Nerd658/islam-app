[2026-08-02] — Fix: Resolve browser audio block with no-referrer policy and dual TTS speech synthesis

What changed:
- Configured `audio.referrerPolicy = 'no-referrer'` on HTML5 Audio elements across `ArabicAlphabet.jsx`, `ArabicVocabulary.jsx`, and `ArabicTajweed.jsx`.
- Dual-engine playback: invokes `SpeechSynthesis` with Arabic voice picker and streams `Google TTS` without sending domain origin referrer headers.

Why:
- Fixes `NotSupportedError: Failed to load because no supported source was found` caused by browser referrer security restrictions on Google TTS endpoints.

Files affected:
- FRONTEND/islam app/src/pages/arabic/ArabicAlphabet.jsx
- FRONTEND/islam app/src/pages/arabic/ArabicVocabulary.jsx
- FRONTEND/islam app/src/pages/arabic/ArabicTajweed.jsx

Breaking changes:
- None.
