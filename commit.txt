[2026-08-02] — Feature: Add Word-by-Word Live Audio Synchronization & Auto-Scroll

What changed:
- Integrated word breakdown API (`/verses/by_chapter/${id}?words=true`) into `QuranReader.jsx`.
- Added real-time `timeupdate` listener to compute exact `currentWordIndex` based on audio playback progress.
- Added live word highlighting: the exact word being spoken by the reciter (Mishary) glows brightly in emerald with scaling and background pulse.
- Added top audio progress bar inside active verse card.
- Added smooth auto-scrolling (`scrollIntoView`) to follow the active verse as recitation progresses.

Why:
- Allows users to follow the recitation word-by-word with precision, making it easy to learn correct pronunciation and stay focused.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
