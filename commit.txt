[2026-08-07] — Enhancement: Remove Quran duplicate from LearnHub

What changed:
- Removed "Le Saint Coran" from the features list in `LearnHub.jsx`.

Why:
- The Quran was recently promoted to the main navigation bar as a dedicated tab, rendering its presence in the "Apprendre" hub redundant. Removing it declutters the Hub and avoids duplicate paths.

Files affected:
- src/pages/LearnHub.jsx

Breaking changes:
- None.

Notes for review:
- Verify that the Quran button no longer appears inside the "Apprendre" tab, but is still accessible via the main navigation bar.
