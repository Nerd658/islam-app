[2026-08-01] — Fix: Restored Chatbot UI and added .env.example (v2.1.1)

What changed:
- Created the missing `Chat.jsx` interface for the "Imam Virtuel" (Chatbot).
- Added the "Imam" icon to the bottom navigation bar.
- Created `BACKEND/.env.example` to guide developers on setting up the `OPENROUTER_API_KEY`.

Why:
- During the React Router refactoring, the Chatbot UI was omitted. It is now fully integrated, responsive, and wired to the secure rate-limited backend `/api/chat` route.

Files affected:
- BACKEND/.env.example (New)
- FRONTEND/islam app/src/pages/Chat.jsx (New)
- FRONTEND/islam app/src/App.jsx (Updated Router)
- FRONTEND/islam app/src/components/Navigation.jsx (Added tab)

Breaking changes:
- None.

Notes for review:
- The Chatbot requires a valid OpenRouter API key. Copy `.env.example` to `.env` in the BACKEND folder and insert your key.
