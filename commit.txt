[2026-08-02] — Fix: Pre-warm speech synthesis voices and implement multi-CDN audio fallback

What changed:
- Created `src/utils/audio.js` with pre-warmed voice caching on browser initialization (`window.speechSynthesis.onvoiceschanged`).
- Implemented multi-CDN audio fallback (Google TTS stream + Youdao TTS fallback) so audio triggers reliably even under restrictive browser autoplay or CORS policies.
- Updated `ArabicAlphabet.jsx`, `ArabicVocabulary.jsx`, and `ArabicTajweed.jsx` to use the shared `playArabicAudio` utility.

Why:
- Fixes issue where SpeechSynthesis `getVoices()` was returning an empty array on cold-start click, resulting in silent playback.

Files affected:
- FRONTEND/islam app/src/utils/audio.js
- FRONTEND/islam app/src/pages/arabic/ArabicAlphabet.jsx
- FRONTEND/islam app/src/pages/arabic/ArabicVocabulary.jsx
- FRONTEND/islam app/src/pages/arabic/ArabicTajweed.jsx

Breaking changes:
- None.
