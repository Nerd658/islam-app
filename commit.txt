[2026-08-02] — UI/UX: HTML-safe word-by-word Tajweed tokenization with glowing underline

What changed:
- Applied regex tokenizer `/((?:<[^>]+>|[^<>\s])+)/g` to extract individual word HTML strings without breaking embedded `<tajweed class=...>` tags.
- Re-enabled live word-by-word highlighting: active word gets a prominent glowing emerald underline (`border-b-4 border-emerald-400 scale-110 shadow-lg shadow-emerald-500/30`), while preserving all underlying Tajweed letter colors 100% intact.

Why:
- Allows users to see word-by-word audio progress with glowing underlines while maintaining all authentic Tajweed letter rules visible.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
