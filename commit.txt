[2026-08-02] — Fix & Audit: Apply Security, Performance & Code Quality recommendations

What changed:
1. Syntax & Code Quality: Fixed `flex:` typo in `Home.jsx` to `finally { setLoading(false); }`.
2. Backend Security & Validation: Updated `chatSchema` in `validators.js` to validate `history` payload from frontend. Added `app.set('trust proxy', 1)` and `xss-clean` middleware in `app.js`. Masked 500 error stack traces in `errorHandler.js` for production.
3. Performance & Memory Leak: Added unmount cleanup logic to pause and reset audio playback in `QuranReader.jsx`.
4. Typography & Aesthetics: Added Google Fonts `Amiri` and `Inter` in `index.html` and configured `.font-arabic` utility class in `index.css` for authentic Arabic text rendering.
5. Code Splitting: Implemented `React.lazy()` and `Suspense` for all page routes in `App.jsx` to optimize initial bundle size.

Why:
- Addressed audit findings across security, performance, typography, and code quality while keeping third-party API calls client-side to maintain a lightweight backend.

Files affected:
- FRONTEND/islam app/src/pages/Home.jsx
- BACKEND/middlewares/validators.js
- BACKEND/app.js
- BACKEND/middlewares/errorHandler.js
- FRONTEND/islam app/src/components/QuranReader.jsx
- FRONTEND/islam app/index.html
- FRONTEND/islam app/src/index.css
- FRONTEND/islam app/src/App.jsx

Breaking changes:
- None.
