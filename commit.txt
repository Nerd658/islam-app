[2026-08-02] — Fix: Complete coherence between count and total session reset in Tasbih

What changed:
- Synchronized `handleReset` and phrase changes in `Tasbih.jsx` to reset both `count` and `total` to `0`, persisting both to `localStorage`.

Why:
- Eliminates UI incoherence where `Total session` showed non-zero values when `Count` was `0000`.

Files affected:
- FRONTEND/islam app/src/pages/Tasbih.jsx

Breaking changes:
- None.
