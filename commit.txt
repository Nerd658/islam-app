[2026-08-07] — Enhancement: Rich and detailed Prophet Stories

What changed:
- Completely rewrote `stories.json` to feature long, detailed, and historically accurate narratives for the prophets.
- Expanded the stories of Youssef and Nuh with much richer content and distinct chapters.
- Added 3 new full stories: Musa (Moïse), Ibrahim (Abraham), and Ayoub (Job).
- Updated `Stories.jsx` to render multi-paragraph text correctly by splitting `content_fr` by `\n` and wrapping them in `<p>` tags with appropriate spacing.

Why:
- The initial placeholder stories were too short and lacked depth. Since there is no reliable French API for detailed Islamic stories, the local JSON was expanded manually to provide a truly engaging reading experience (15-25 minutes read time per story) as requested by the user.
- The UI fix was necessary because React does not render newline characters inside a single text node without CSS `white-space` adjustments or explicit splitting.

Files affected:
- src/data/stories.json
- src/pages/Stories.jsx

Breaking changes:
- None.

Notes for review:
- Open a story and ensure the paragraphs are well-spaced and readable.
