[2026-08-02] — Fix: Smart URL detection in playArabicAudio resolves parameter mismatch

What changed:
- **src/utils/audio.js**: Implemented smart parameter detection (`param2.startsWith('http')`) so string fallbacks (like "Ta") are never misinterpreted as broken audio file paths in Howler.js.
- **ArabicAlphabet.jsx**: Passed `letter.exampleAudio` to `playAudio` for direct human MP3 recitation playback.

Why:
- Fixes issue where non-URL string fallbacks passed to Howler.js caused silent HTTP load failures.

Files affected:
- FRONTEND/islam app/src/utils/audio.js
- FRONTEND/islam app/src/pages/arabic/ArabicAlphabet.jsx

Breaking changes:
- None.
