[2026-08-01] — UI/UX: Refactor Adhkar page to Vercel-style and remove emojis

What changed:
- Removed emojis (🌅, 🌙) from `Adhkar.jsx` and replaced them with `lucide-react` icons (`Sun`, `Moon`).
- Fixed a bug where `AdhkarList.jsx` was duplicating the "Matin / Soir" buttons. The state is now cleanly passed down as a prop.
- Completely redesigned `AdhkarList.jsx` to match the new Vercel-style Pro design (sleek black cards, gray borders, white text) instead of the old colorful gradients.

Why:
- The user pointed out the stickers were still on this page. Also, the UI was out of sync with the recent "Pro" design refactoring.

Files affected:
- FRONTEND/islam app/src/pages/Adhkar.jsx
- FRONTEND/islam app/src/components/AdhkarList.jsx

Breaking changes:
- None.
