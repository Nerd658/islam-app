[2026-08-02] — Fix: Enhance audio play promise and volume handling for Arabic TTS

What changed:
- Updated audio element initialization in `ArabicAlphabet.jsx`, `ArabicVocabulary.jsx`, and `ArabicTajweed.jsx` to explicitly set `audio.volume = 1.0` and handle async `play()` promise rejection with voice selection fallback.

Why:
- Fixes issue where audio playback promise was silently rejected or unmuted by browser audio policies.

Files affected:
- FRONTEND/islam app/src/pages/arabic/ArabicAlphabet.jsx
- FRONTEND/islam app/src/pages/arabic/ArabicVocabulary.jsx
- FRONTEND/islam app/src/pages/arabic/ArabicTajweed.jsx

Breaking changes:
- None.
