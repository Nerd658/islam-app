[2026-08-01] — Feature: Progressive Web App (Offline Mode)

What changed:
- Installed and configured `vite-plugin-pwa` in `vite.config.js`.
- Enabled the application to be installed on mobile devices and desktops as a native-like app.
- Prepared the manifest (name, description, theme color, icons) to support offline caching.

Why:
- A Muslim user may often find themselves in situations without internet access (e.g., inside a mosque with thick walls or while traveling). Enabling offline mode via PWA ensures they can still access their daily Adhkar and previously loaded prayer times.
- This dramatically increases the utility of the application for the community.

Files affected:
- FRONTEND/islam app/package.json — Added `vite-plugin-pwa` dependency.
- FRONTEND/islam app/vite.config.js — Configured the `VitePWA` plugin with manifest details.

Breaking changes:
- None.

Notes for review:
- To fully leverage the PWA in production, ensure the `vite.svg` icon is properly served or replaced by a high-res Islamic app logo later. The app will now prompt users to install it when they visit the web version.
