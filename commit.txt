[2026-08-01] — Feature: Add dedicated Goals & Daily Program page (/goals)

What changed:
- Created a new dedicated page `Goals.jsx` accessible at `/goals` featuring structured daily spiritual objectives (Reading 1 Hizb, Adhkar, 100 Istighfar, Meditating on Allah's Names).
- Includes category cards with progress counters, interactive goal completion checkboxes (persisted in `localStorage`), and direct shortcut buttons leading to each respective section (`/quran`, `/adhkar`, `/tasbih`, `/names`).
- Updated `Navigation.jsx` to add the "Objectifs" menu item (`Target` icon) and cleaned up the sidebar.
- Added `/goals` route in `App.jsx`.

Why:
- Replaced the crammed sidebar widget with a dedicated, spacious, and premium page as requested by the user.

Files affected:
- FRONTEND/islam app/src/pages/Goals.jsx (Added)
- FRONTEND/islam app/src/components/Navigation.jsx
- FRONTEND/islam app/src/App.jsx

Breaking changes:
- None.
