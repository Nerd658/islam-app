[2026-08-02] — Fix: Implement zero-latency dual-tier audio engine for instant letter sound playback

What changed:
- **src/utils/audio.js**: Separated audio into Tier 1 (Howler.js for real human Qari MP3 URLs) and Tier 2 (Instant zero-latency SpeechSynthesis for individual letters and vocalization marks).

Why:
- Fixes issue where network TTS endpoints failed to load over HTML5 CORS streams, ensuring letter taps trigger instantaneous sound on every browser and device.

Files affected:
- FRONTEND/islam app/src/utils/audio.js

Breaking changes:
- None.
