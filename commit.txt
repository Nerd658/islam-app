[2026-08-02] — Fix: Resolve Javascript Garbage Collector Audio Destruction Bug

What changed:
- **src/utils/audio.js**:
  - Bound `Audio` object to persistent global `window._activeArabicAudio` reference to prevent JavaScript Garbage Collector from destroying the audio instance mid-stream.
  - Added explicit previous audio cleanup (`pause()` and `currentTime = 0`).
- **ArabicAlphabet.jsx**:
  - Bound letter click handlers to pronounce the exact letter sound (`selectedLetter.letter`).

Why:
- Fixes issue where `new Audio()` was silently destroyed by browser Garbage Collector mid-stream without throwing console errors.

Files affected:
- FRONTEND/islam app/src/utils/audio.js
- FRONTEND/islam app/src/pages/arabic/ArabicAlphabet.jsx

Breaking changes:
- None.
