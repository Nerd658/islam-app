2026-08-07 — Refactor Stories UI, Add StoryReader, and Update App Logo

What changed:
- Overhauled Tasbih UI with premium design tokens (emerald glow, LED drop-shadows, gold milestone badge).
- Implemented pagination for the Stories list (6 items per page).
- Extracted the story reading modal into a dedicated `StoryReader` route (`/stories/:type/:slug`).
- Replaced the default React/Vite logo with a custom Islamic SVG logo (crescent and star).

Why:
- Deep linking support and improved mobile UX for long-form reading (avoids `fixed bottom-0` navigation bar conflicts).
- Consistent branding and aesthetics (SVG logo and tokenized colors in Tasbih and Stories).

Files affected:
- FRONTEND/islam app/src/pages/Tasbih.jsx — Applied `theme-primary` gradients and glow filters.
- FRONTEND/islam app/src/pages/Stories.jsx — Removed modal logic, added pagination and routing.
- FRONTEND/islam app/src/pages/StoryReader.jsx — Created dedicated reading component.
- FRONTEND/islam app/src/App.jsx — Registered `StoryReader` route.
- FRONTEND/islam app/index.html — Replaced `vite.svg` with `logo.svg`.
- FRONTEND/islam app/vite.config.js — Updated PWA manifest icon paths.
- FRONTEND/islam app/public/logo.svg — Created custom SVG logo.
- FRONTEND/islam app/public/vite.svg — Deleted default logo.

Breaking changes:
- None.

Notes for review:
- Verify deep linking to stories (e.g., `/stories/prophets/yusuf`) works correctly when the app is installed as a PWA.
