[2026-08-02] — UI/UX: Make Quran reader header 100% sticky on page scroll

What changed:
- Configured `sticky top-0 z-50 bg-[#0a0a0a]/95 backdrop-blur-md` directly attached to the main scrolling document flow.
- "Retour aux Sourates", "FR", "Tajweed", "Télécharger", "Règles Tajweed", and "Surah Title" now stick frozen to the top of the browser viewport when scrolling through verses.

Why:
- Fixes issue where nested scroll overflow prevented sticky positioning from locking to the top of the page.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
