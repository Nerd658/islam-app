[2026-08-02] — Fix: Restart backend with /api/tts proxy route and cross-origin Helmet policy

What changed:
- **BACKEND/app.js**:
  - Configured `helmet` with `crossOriginResourcePolicy: { policy: 'cross-origin' }` to resolve `ERR_BLOCKED_BY_RESPONSE.NotSameOrigin`.
  - Added missing `hpp` and `rateLimit` module imports.
  - Restarted backend server process on port 4000.

Why:
- Fixes the 404 and CORS NotSameOrigin errors on `http://localhost:4000/api/tts` so crystal-clear Arabic audio streams cleanly to the frontend.

Files affected:
- BACKEND/app.js

Breaking changes:
- None.
