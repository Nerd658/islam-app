[2026-08-02] — Fix: Synchronize reset actions directly with localStorage in Tasbih

What changed:
- Updated `handleReset` and `onChange` (phrase selector) in `Tasbih.jsx` to explicitly write `localStorage.setItem('tasbih_count', '0')`.

Why:
- Ensures pressing the reset button immediately syncs `tasbih_count` to 0 in `localStorage`, so reloads stay at 0 after a reset.

Files affected:
- FRONTEND/islam app/src/pages/Tasbih.jsx

Breaking changes:
- None.
