[2026-08-02] — Fix: Refresh stale offline cache when missing French translation data

What changed:
- Updated `fetchVerses` cache validator in `QuranReader.jsx` to verify that cached items include `v.translation`. Stale cache entries lacking translation will automatically re-fetch live data.
- Switched translation JSX element to use `dangerouslySetInnerHTML={{ __html: v.translation }}` for HTML formatting support.

Why:
- Resolves issue where previously cached Surahs without translation fields prevented French text from rendering.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
