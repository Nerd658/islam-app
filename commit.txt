[2026-08-01] — Refactor: Make Adhkar fully dynamic and DRY

What changed:
- Extracted the UI logic for an invocation card into a new highly reusable `DhikrCard.jsx` component (DRY principle).
- Updated `adhkar.json` with a massive list of invocations covering 9 distinct categories (Matin, Soir, Sommeil, Réveil, Repas, Voyage, Prière, Maison, Tristesse).
- Completely rewrote `Adhkar.jsx`: it now dynamically reads all unique categories from `adhkar.json` and renders a horizontal scrollable menu (chips), abandoning the hardcoded "Matin/Soir" buttons.
- `AdhkarList.jsx` now strictly filters the dataset based on the selected dynamic category and renders clean `DhikrCard` components.

Why:
- The user pointed out the need to support an infinite number of categories (travel, eating, etc.) instead of just morning/evening. 
- The user explicitly requested to respect DRY by creating reusable components rather than duplicating huge chunks of code.

Files affected:
- FRONTEND/islam app/src/components/DhikrCard.jsx (Added)
- FRONTEND/islam app/src/components/AdhkarList.jsx
- FRONTEND/islam app/src/pages/Adhkar.jsx
- FRONTEND/islam app/src/data/adhkar.json

Breaking changes:
- None.
