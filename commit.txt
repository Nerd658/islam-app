[2026-08-02] — Audio Sync: Calibrate audio lead-in and trailing silence offsets

What changed:
- Added `startOffset = 0.25s` and `effectiveDur = dur - 0.5s` in `QuranReader.jsx`'s `handleTimeUpdate` function.

Why:
- Accounts for initial breath silence and trailing MP3 silence in Mishary's recitation files, aligning word highlight with voice onset.

Files affected:
- FRONTEND/islam app/src/components/QuranReader.jsx

Breaking changes:
- None.
