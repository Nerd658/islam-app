[2026-08-02] — Feature: Bundle static offline MP3 audio assets for all 28 Arabic letters

What changed:
- **public/audio/letters/**: Generated 28 static high-clarity MP3 audio assets (`1.mp3` to `28.mp3`) for every Arabic letter name (`أَلِف`, `بَاء`, `تَاء`, `خَاء` [guttural Khâ], etc.).
- **arabic_learning.json**: Bound `letterAudio` to `/audio/letters/{id}.mp3`.
- **ArabicAlphabet.jsx**: Letter clicks now instantly trigger the bundled static MP3 audio with 0ms delay and zero network dependency.

Why:
- Replaces network/TTS/offset dependencies with 100% reliable, offline-ready bundled MP3 audio assets for every letter.

Files affected:
- FRONTEND/islam app/public/audio/letters/
- FRONTEND/islam app/src/data/arabic_learning.json
- FRONTEND/islam app/src/pages/arabic/ArabicAlphabet.jsx

Breaking changes:
- None.
