[2026-08-07] — Enhancement: Adhkar Search and PWA Layout

What changed:
- Added a global search bar to the Adhkar page (`Adhkar.jsx`) to filter invocations by Arabic text, French translation, or source reference.
- Added a "Toutes" (All) category option, selected by default, allowing users to search across the entire dataset of 78 invocations.
- Updated `AdhkarList.jsx` to process the `searchQuery` prop and perform case-insensitive matching across multiple fields.
- Improved PWA mobile layout alignment by ensuring inputs use `text-base` (prevents iOS Safari zoom) and adding `pb-32` to the list container for better bottom-nav clearance.

Why:
- The user requested a search feature for the Adhkar page to quickly find specific invocations without manually browsing through 17 categories.
- PWA layout adjustments were requested to ensure a native-like experience on mobile devices without layout shifts or obscured content.

Files affected:
- src/pages/Adhkar.jsx — UI updates for search input and 'Toutes' category.
- src/components/AdhkarList.jsx — Filtering logic updated to support search query.

Breaking changes:
- None.

Notes for review:
- Verify that searching works seamlessly across all categories.
- Check mobile PWA rendering to ensure the bottom padding (`pb-32`) is sufficient.
