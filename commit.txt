[2026-08-02] — Fix: Implement phonetic audio fallback for system environments without OS Arabic voices

What changed:
- **src/utils/audio.js**: Added phonetic fallback parameter (`phoneticFallback`). If the client OS lacks an installed Arabic voice pack, `SpeechSynthesis` automatically speaks the clear phonetic equivalent ("Taa", "Baa", "Alif", "Jim", etc.).
- **ArabicAlphabet.jsx**: Passed `letter.name` ("Ta", "Ba", "Alif", etc.) as secondary fallback to `playArabicAudio`.

Why:
- Guarantees 100% audible pronunciation across all operating systems, Linux distros, and browser privacy configurations.

Files affected:
- FRONTEND/islam app/src/utils/audio.js
- FRONTEND/islam app/src/pages/arabic/ArabicAlphabet.jsx

Breaking changes:
- None.
