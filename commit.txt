[2026-08-01] — UI/UX: Floating dropdown style for city search suggestions

What changed:
- Redesigned `SearchInput.jsx` with an integrated `Search` icon and Vercel dark styling.
- Transformed `LocationSuggestions.jsx` into an absolute floating dropdown menu (`absolute top-full z-50`) positioned directly below the search input field.
- Added `MapPin` icon to each city suggestion item for enhanced clarity and removed old gradient background styles.
- Updated `Home.jsx` to wrap search input and suggestions in a relative container.

Why:
- Previously, search suggestions pushed down page content and used generic gradients.
- The new floating dropdown provides a smooth, non-intrusive, and modern experience.

Files affected:
- FRONTEND/islam app/src/components/SearchInput.jsx
- FRONTEND/islam app/src/components/LocationSuggestions.jsx
- FRONTEND/islam app/src/pages/Home.jsx

Breaking changes:
- None.
